<h2>忘記您的密碼?</h2>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= devise_error_messages! %>

  <div class="field">
    <%= f.label :電子信箱 %><br />
    <%= f.email_field :email, autofocus: true %>
  </div>

  <div class="actions">
    <%= f.submit "寄重置密碼資訊至我的信箱" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
